#include<stdio.h>

void main()
{
	char name[30];

	printf("enter your value :");
	scanf("%s",&name);
	int length=0,i;
	for(i=0;i<=name[i];i++)
	{
		length++;
		
	}
	printf("%d",length);
}